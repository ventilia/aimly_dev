-- V10__add_keyword_variants.sql
-- Добавляем колонку variants в таблицу keywords.
-- Хранит AI-сгенерированные морфологические варианты через запятую.
-- Пример: "ищем дизайнера,нужен дизайнер,нужна дизайнера,требуется дизайнер"
-- NULL = варианты ещё не сгенерированы (строки добавленные до этой миграции).

ALTER TABLE keywords ADD COLUMN IF NOT EXISTS variants TEXT;